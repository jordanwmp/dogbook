import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    label:{
        textAlign: 'left',
        fontWeight: 'bold',
        fontSize: 18,
        marginBottom: 20
    }
})

export default styles